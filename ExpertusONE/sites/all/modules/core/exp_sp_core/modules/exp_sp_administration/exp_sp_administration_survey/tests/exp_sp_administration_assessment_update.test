<?php 

class AdministrationAssessmentUpdateBase extends AdministrationAssessmentTestHelper
{
	public function updateAssessmentTestCase($scope = 'all') {
		$ctoolModel = array(
				'modelWidth' => 890,
				'modelClass' => 'ctools-modal-ctools-admin-survey-addedit-style',
				'modelTitle' => t('Assessment'),
				'contentWidth' => 548,		// from #modal-content
				'contentMhight' => 235,		// from #modal-content
				'outerHeight' => 378,
				'outerWidth' => 593,
				'blockHeight' => 1232,		// Document Height
				'blockWidth' => 1343,		// Document Height
				'modelTop' => 72,			// from #modalContent
				'modelLeft' => 630,			// from #modalContent
				'docHeight' => 1232,		// Document Height
				'docWidth' => 1343			// Document Width
		);
		$this->addCtoolModelContent($ctoolModel);

		$modelContent = $this->content;
		
		// Based on the scope set the data to test
		if ($scope == 'all') {
			$testCases = $this->testcases['assessmentUpdate'];
		} else {
			$testCases = array($this->testcases['assessmentUpdate'][$scope]);
		}

		foreach($testCases as $test){
			//Reset content with the ctool model
			$this->content = $modelContent;

			//Get empty form
			$post_field = array('wrapper'=>'paint-narrow-search-results');
			$update_id = $test['id'];
			unset($test['id']);
			$out = $this->drupalPostAJAX(null, $edit,'Save','?q=/administration/assessment/assessmentdetails/ajax/addedit/' . $update_id, array(), array(), '', $post_field);

			$this->verbose('Request to: ' . $this->getUrl() . '<hr />' . $this->content);

			// Do General validations
			$this->assertResponse(200,'Form Rended successfully');
			$webError = "Website encountered an unexpected error";
			$this->assertNoText(t($webError),'Web errors not found','catalog_admin');

			// Save form
			$param = array(
					"form_id"=>"exp_sp_administration_survey_assessment_addedit_form",
					"_triggering_element_name" 	=>"save",
					"_triggering_element_value"	=>"Save",
			);

			$post_field = array('submit'=>$param,'wrapper'=>'catalog-survey-assesment-basic-addedit-form');

			$out = $this->drupalPostAJAX(null, $test,'Save','?q=system/ajax',array(),array(),'',$post_field);


			$this->verbose('Request to: ' . $this->getUrl() .	'<hr />' . $this->content);


			//Check if any error thrown
			$isError = $this->xpath('//div[@class="messages error"]//ul/li');
			$error = '';
			debug("ERROR STRINGS ");

			if(sizeOf($isError) > 0){
				$error = (string) $isError[0];
			}
			$this->assertResponse(200,'Form Repainted successfully');
			$txt = 'updated successfully';
			$this->assertText(t($txt),'Updated changes successfully','catalog_admin');
			$this->assertNoText(t($error),'Error - '.$error,'catalog_admin');
			$webError = "Website encountered an unexpected error";
			$this->assertNoText(t($webError),'Web errors not found','catalog_admin');
		}
	}
}


class AdministrationAssessmentUpdateAll extends AdministrationAssessmentUpdateBase
{
	public static function getInfo() {
		return array(
				'name' => 'Administration - Assessment - Update - All Tests',
				'description' => 'Module : exp_sp_administration_people',
				'group' => '1401 - Administration - Assessment - AssessmentDetails - Update',
		);
	}

	public function testAssessmentUpdateAll() {
		$this->manageAdminAssessment();
		debug('testAssessmentUpdateAll called');
		$this->updateAssessmentTestCase('all');
	}
}

class AdministrationAssessmentUpdateCase1 extends AdministrationAssessmentUpdateBase
{
	public static function getInfo() {
		return array(
				'name' => 'Administration - Assessment - Update - Case 1',
				'description' => 'Module : exp_sp_administration_people',
				'group' => '1401 - Administration - Assessment - AssessmentDetails - Update',
		);
	}

	public function testAssessmentUpdateCase1() {
		$this->manageAdminAssessment();
		debug('testAssessmentUpdateCase1 called');
		$this->updateAssessmentTestCase('st_case1');
	}
}

class AdministrationAssessmentUpdateCase2 extends AdministrationAssessmentUpdateBase
{
	public static function getInfo() {
		return array(
				'name' => 'Administration - Assessment - Update - Case 2',
				'description' => 'Module : exp_sp_administration_people',
				'group' => '1401 - Administration - Assessment - AssessmentDetails - Update',
		);
	}

	public function testAssessmentUpdateCase2() {
		$this->manageAdminAssessment();
		debug('testAssessmentUpdateCase2 called');
		$this->updateAssessmentTestCase('st_case2');
	}
}


?>