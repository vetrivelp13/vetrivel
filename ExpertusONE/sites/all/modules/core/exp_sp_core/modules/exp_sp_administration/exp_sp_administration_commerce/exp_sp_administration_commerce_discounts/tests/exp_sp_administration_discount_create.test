<?php 
class AdministrationDiscountCreateBase extends AdministrationDiscountTestHelper
{
	public function createDiscountTestCase($scope = 'all') {
		$ctoolModel = array(
				'modelWidth' => 890,
				'modelClass' => 'ctools-modal-ctools-admin-resource-addedit-style',
				'modelTitle' => t('Discount'),
				'contentWidth' => 880,
				'contentMhight' => 489,
				'outerHeight' => 620,
				'outerWidth' => 336,
				'blockHeight' => 1232,
				'blockWidth' => 1343,
				'modelTop' => 150,
				'modelLeft' => 226.5,
				'docHeight' => 1232,
				'docWidth' => 1343
		);
		$this->addCtoolModelContent($ctoolModel);

		$modelContent = $this->content;
		
		// Based on the scope set the data to test
		if ($scope == 'all') {
			$testCases = $this->testcases['discountCreate'];
		} else {
			$testCases = array($this->testcases['discountCreate'][$scope]);
		}

		foreach($testCases as $test){
			
			//Reset content with the ctool model
			$this->content = $modelContent;
				
			//Get empty form
			$post_field = array('wrapper'=>'paint-narrow-search-results');

			$out = $this->drupalPostAJAX(null, $edit,'Save','?q=administration/commerce/discounts/ajax/addedit',array(),array(),'',$post_field);

			$this->verbose('Request to: ' . $this->getUrl() . '<hr />' . $this->content);

			// Do General validations
			$this->assertResponse(200,'Form Rended successfully');
			$webError = "Website encountered an unexpected error";
			$this->assertNoText(t($webError),'Web errors not found','catalog_admin');
			
			$testData = array('dis_type' => $test['dis_type']);
			// select a country field to populate state values.
			$this->drupalPostAJAX(null, $testData, 'dis_type');
				
			

			// Save form
			$param = array(
					"form_id"=>"exp_sp_administration_discounts_addedit_form",
					"_triggering_element_name" 	=>"saveandpublish",
					"_triggering_element_value"	=>"Save and Activate",
			);
				
			$post_field = array('submit'=>$param,	'wrapper'=>'discount-basic-addedit-form');

			$out = $this->drupalPostAJAX(null, $test,'Save','?q=system/ajax',array(),array(),'',$post_field);
				
				
			$this->verbose('Request to: ' . $this->getUrl() .	'<hr />' . $this->content);


			//Check if any error thrown
			$isError = $this->xpath('//div[@class="messages error"]//ul/li');
			$error = '';
			debug("ERROR STRINGS ");

			if(sizeOf($isError) > 0){
				$error = (string) $isError[0];
			}
			$this->assertResponse(200,'Form Repainted successfully');
			$txt = 'saved successfully';
			$this->assertText(t($txt),'saved successfully','catalog_admin');
			$this->assertNoText(t($error),'Error - '.$error,'catalog_admin');
			$webError = "Website encountered an unexpected error";
			$this->assertNoText(t($webError),'Web errors not found','catalog_admin');
		}
	}

}

class AdministrationDiscountCreateAll extends AdministrationDiscountCreateBase
{
	public static function getInfo() {
		return array(
				'name' => 'Administration - Discount - Create - All Tests',
				'description' => 'Module : exp_sp_administration_discount',
				'group' => '1702 - Administration - Manage - Discount - Create',
		);
	}
	
	public function testDiscountCreateAll() {
		$this->manageAdminDiscount();
		debug('testAdminDiscountCreateAll called');
		$this->createDiscountTestCase('all');
	}
}

class AdministrationDiscountCreateCase1 extends AdministrationDiscountCreateBase
{
	public static function getInfo() {
		return array(
				'name' => 'Administration - Discount - Create - Case 1',
				'description' => 'Module : exp_sp_administration_discount',
				'group' => '1702 - Administration - Manage - Discount - Create',
		);
	}

	public function testDiscountCreateCase1() {
		$this->manageAdminDiscount();
		debug('testDiscountCreateCase1 called');
		$this->createDiscountTestCase('st_case1');
	}
}

class AdministrationDiscountCreateCase2 extends AdministrationDiscountCreateBase
{
	public static function getInfo() {
		return array(
				'name' => 'Administration - Discount - Create - Case 2',
				'description' => 'Module : exp_sp_administration_discount',
				'group' => '1702 - Administration - Manage - Discount - Create',
		);
	}

	public function testDiscountCreateCase2() {
		$this->manageAdminDiscount();
		debug('testDiscountCreateCase2 called');
		$this->createDiscountTestCase('st_case2');
	}
}
?>