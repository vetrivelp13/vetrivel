
  <div class="field text h5peditor-interaction-text">
   <label class="h5peditor-label-wrapper"><span class="h5peditor-label h5peditor-required"><%=window.parent.Drupal.t('LBL3032')%>:</span><textarea id="edit-short-description-value" rows=3 cols=40 class="h5peditor-text" value=""><%=GLOBALPARAMS_H5P.action.params.text%></textarea></label>
   <div class="h5peditor-field-description"></div>
  </div> 
 <%
	if(GLOBALPARAMS_H5P.action.params.contenttype != "presentation")
	{
%> 
  <div class="field text h5peditor-interaction-label">
   <label class="h5peditor-label-wrapper"><span class="h5peditor-label"><%=window.parent.Drupal.t('LBL3034')%>:</span><input class="h5peditor-text" id="labelid" type="text" value="<%=GLOBALPARAMS_H5P.label%>" maxlength="100"></label>
   <div class="h5peditor-field-description">Label displayed next to interaction icon.</div>
  </div> 
 <%
 }
  %>
  
   	
   
   <script>
   function editorOnClick(id)
   {
	try{
	if(document.getElementById('cursor_id')!= null){//id == 'edit-message-value'){
		$('#cursor_id').val(id);
	}
	$('#message-container').remove();
	}catch(e){
			// To Do
		}
   }
   
   
  		if(GLOBALPARAMS_H5P.displayType == "poster")
  		{
  			ns.$("#labelid").parent().parent().hide();
  		}
		vtip();
		$(document).ready(function(){
			tinymce.editors = [];
			tinymce.EditorManager.editors = [];
			$("#edit-short-description-value_parent").remove(); 
				var editor_id = "edit-short-description-value";
				tinymce.EditorManager.execCommand('mceRemoveControl',true, editor_id);
					tinymce.init(Drupal.settings.wysiwyg.configs.tinymce);			
			window.setTimeout(function(){
				//$(".defaultSkin").not(':last').remove();
			},100);
	
		})
		
   </script>
