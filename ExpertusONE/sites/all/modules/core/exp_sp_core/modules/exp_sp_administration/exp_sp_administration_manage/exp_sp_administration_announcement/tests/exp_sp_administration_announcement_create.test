<?php 
class AdministrationAnnouncementCreateBase extends AdministrationAnnouncementTestHelper
{
	public function createAnnouncementTestCase($scope = 'all') {
		$ctoolModel = array(
				'modelWidth' => 890,
				'modelClass' => 'ctools-modal-ctools-admin-announcement-notice-addedit-style',
				'modelTitle' => t('Announcement'),
				'contentWidth' => 570,
				'contentMhight' => 335,
				'outerHeight' => 620,
				'outerWidth' => 336,
				'blockHeight' => 1232,
				'blockWidth' => 1343,
				'modelTop' => 150,
				'modelLeft' => 226.5,
				'docHeight' => 1232,
				'docWidth' => 1343
		);
		$this->addCtoolModelContent($ctoolModel);

		$modelContent = $this->content;
		
		// Based on the scope set the data to test
		if ($scope == 'all') {
			$testCases = $this->testcases['announcementCreate'];
		} else {
			$testCases = array($this->testcases['announcementCreate'][$scope]);
		}

		foreach($testCases as $test){
			
			//Reset content with the ctool model
			$this->content = $modelContent;
				
			//Get empty form
			$post_field = array('wrapper'=>'paint-narrow-search-results');

			$out = $this->drupalPostAJAX(null, $edit,'Save','?q=/administration/manage/announcement/ajax/addedit',array(),array(),'',$post_field);

			$this->verbose('Request to: ' . $this->getUrl() . '<hr />' . $this->content);

			// Do General validations
			$this->assertResponse(200,'Form Rended successfully');
			$webError = "Website encountered an unexpected error";
			$this->assertNoText(t($webError),'Web errors not found','catalog_admin');
			
			
			// Save form
			$param = array(
					"form_id"=>"exp_sp_administration_announcement_addedit_form",
					"_triggering_element_name" 	=>"saveandpublish",
					"_triggering_element_value"	=>"Save and Publish",
			);
				
			$post_field = array('submit'=>$param,	'wrapper'=>'announcement-basic-addedit-form');

			$out = $this->drupalPostAJAX(null, $test,'Save','?q=system/ajax',array(),array(),'',$post_field);
				
				
			$this->verbose('Request to: ' . $this->getUrl() .	'<hr />' . $this->content);


			//Check if any error thrown
			$isError = $this->xpath('//div[@class="messages error"]//ul/li');
			$error = '';
			debug("ERROR STRINGS ");

			if(sizeOf($isError) > 0){
				$error = (string) $isError[0];
			}
			$this->assertResponse(200,'Form Repainted successfully');
			$txt = 'updated successfully';
			$this->assertText(t($txt),'updated successfully','catalog_admin');
			$this->assertNoText(t($error),'Error - '.$error,'catalog_admin');
			$webError = "Website encountered an unexpected error";
			$this->assertNoText(t($webError),'Web errors not found','catalog_admin');
		}
	}

}

class AdministrationAnnouncementCreateAll extends AdministrationAnnouncementCreateBase
{
	public static function getInfo() {
		return array(
				'name' => 'Administration - Announcement - Create - All Tests',
				'description' => 'Module : exp_sp_administration_announcement',
				'group' => '1506 - Administration - Manage - Announcement - Create',
		);
	}
	
	public function testAnnouncementCreateAll() {
		$this->manageAdminAnnouncement();
		debug('testAdminAnnouncementCreateAll called');
		$this->createAnnouncementTestCase('all');
	}
}

class AdministrationAnnouncementCreateCase1 extends AdministrationAnnouncementCreateBase
{
	public static function getInfo() {
		return array(
				'name' => 'Administration - Announcement - Create - Case 1',
				'description' => 'Module : exp_sp_administration_announcement',
				'group' => '1506 - Administration - Manage - Announcement - Create',
		);
	}

	public function testAnnouncementCreateCase1() {
		$this->manageAdminAnnouncement();
		debug('testAnnouncementCreateCase1 called');
		$this->createAnnouncementTestCase('st_case1');
	}
}

class AdministrationAnnouncementCreateCase2 extends AdministrationAnnouncementCreateBase
{
	public static function getInfo() {
		return array(
				'name' => 'Administration - Announcement - Create - Case 2',
				'description' => 'Module : exp_sp_administration_announcement',
				'group' => '1506 - Administration - Manage - Announcement - Create',
		);
	}

	public function testAnnouncementCreateCase2() {
		$this->manageAdminAnnouncement();
		debug('testAnnouncementCreateCase2 called');
		$this->createAnnouncementTestCase('st_case2');
	}
}
?>